version: 1
frontend:
  phases:
    preBuild:
      commands:
    build:
      commands:
        - rspec
        - ruby build/write_files.rb
  artifacts:
    baseDirectory: /build
    files:
      - 'index.html'
      - '404.html'
      - 'pictures/**/*'
      - 'words/**/*'
      - 'code/**/*'
  cache:
    paths: []