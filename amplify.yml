version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd build
    build:
      commands:
        - ruby build.rb
  artifacts:
    baseDirectory: /
    files:
      - '!build/**/*'
      - '!static-backups/**/*'
      - '!templates/**/*'
      - '**/*'
  cache:
    paths: []