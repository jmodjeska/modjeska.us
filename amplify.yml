version: 1
frontend:
  phases:
    preBuild:
      commands:
    build:
      commands:
        - gem install rspec
        - rspec
        - pwd
        - ruby build/write_posts.rb
  artifacts:
    baseDirectory: /build
    files:
      - 'index.html'
      - '404.html'
      - 'pictures/**/*'
      - 'words/**/*'
      - 'code/**/*'
  cache:
    paths: []